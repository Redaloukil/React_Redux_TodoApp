import React from 'react';

const Todo = (props) => {
    return(
        <div className="todo">
            <h1>{this.props.data.title}</h1>
            <p>{this.props.data.description}</p>
        </div>
    )
}

export default Todo